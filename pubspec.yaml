name: farmer_crop_care
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # State Management
  flutter_riverpod: ^3.0.3
  riverpod_annotation: 

  # Backend
  supabase_flutter: ^2.0.0

  # Local Database
  drift: ^2.10.0
  sqlite3_flutter_libs: 
  path_provider: 
  path: 

  # Utilities
  intl: 
  uuid: 
  equatable: 
  timezone: 

  # Features
  file_picker: 
  excel: 
  csv: 
  flutter_local_notifications: 
  syncfusion_flutter_pdf: ^32.1.20
  http: ^1.6.0
  geolocator: ^11.0.0
  connectivity_plus: ^5.0.2
  google_nav_bar: ^5.0.6

dev_dependencies:
  flutter_launcher_icons: ^0.13.1

  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: 
  riverpod_generator: 
  drift_dev: 

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon.png"

flutter_native_splash:
  color: "#4CAF50"
  image: "assets/icon.png"
  android: true
  ios: true

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/icon.png

